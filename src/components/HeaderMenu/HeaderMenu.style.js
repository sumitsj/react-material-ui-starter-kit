import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  menuButton: {
    marginRight: '-12px',
  },
  menuButtonIcon: {
    fontSize: '1.625rem',
  },
}));

export default useStyles;
